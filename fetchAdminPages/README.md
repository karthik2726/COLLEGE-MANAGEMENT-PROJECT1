# Fetch Admin Pages Service

This service handles the synchronization and fetching of admin pages from external sources.

## Files

- `service.js` - Main service for fetching admin pages
- `runSync.js` - Script to run synchronization manually

## Usage

To run manual synchronization:
```bash
node fetchAdminPages/runSync.js
```